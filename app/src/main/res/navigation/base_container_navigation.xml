<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/base_container_navigation"
    app:startDestination="@id/lanucherFragment">
    <fragment
        android:id="@+id/lanucherFragment"
        android:name="com.young.akatsuki.ui.LanucherFragment"
        android:label="fragment_lanucher"
        tools:layout="@layout/fragment_lanucher" >
        <action
            android:id="@+id/action_lanucherFragment_to_rateFragment"
            app:destination="@id/rateFragment" />
    </fragment>
    <fragment
        android:id="@+id/rateFragment"
        android:name="c"
        android:label="fragment_rate"
        tools:layout="@layout/fragment_rate" />
</navigation>